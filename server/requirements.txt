# Servidor de tracking do MLflow (Cloud Run, atrás do IAP).
# Versões pinadas para builds reproduzíveis.

# MLflow com extras (inclui dependências de backend/registry usadas pelo server).
mlflow[extras]==3.13.0

# Driver Postgres para o backend store no Cloud SQL.
psycopg2-binary==2.9.12

# Acesso a GCS para o artifact store (default-artifact-root = gs://...).
google-cloud-storage==3.11.0

# Servidor WSGI usado pelo `mlflow server --gunicorn-opts`.
# 23.0.0 instala sem conflito com mlflow[extras]==3.13.0.
gunicorn==23.0.0
