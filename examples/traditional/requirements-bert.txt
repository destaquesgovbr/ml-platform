# Caminho "pesado" (opcional): fine-tune de BERT com HuggingFace Transformers.
# NÃO é instalado nos testes de CI. Instale manualmente para rodar train_bert.py:
#   pip install -r requirements.txt -r requirements-bert.txt
transformers==4.46.2
torch==2.5.1
datasets==3.1.0
accelerate==1.1.1
